{
  "name": "@baby-copilot/core",
  "version": "0.0.1",
  "private": false,
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc -p .",
    "watch": "tsc -w -p .",
    "clean": "rm -rf dist",
    "prepare": "pnpm run build",
    "test": "pnpm run build && node --test"
  },
  "dependencies": {
    "shiki": "^3.7.0",
    "diff-match-patch": "^1.0.5"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "@types/node": "20.x",
    "@types/diff-match-patch": "^1.0.36"
  }
}
